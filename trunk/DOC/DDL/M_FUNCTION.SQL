CREATE TABLE PDM.M_FUNCTION (
	LANGID NVARCHAR2(10) NOT NULL,
	FUNCTIONID NUMBER(3) NOT NULL,
	FUNCTIONNAME NVARCHAR2(100),
	FUNCTIONPATH NVARCHAR2(100),
	CATALOGID NUMBER(2) NOT NULL,
	FUNCTIONINDEX NUMBER(4),
	FUNCTIONIMAGE NVARCHAR2(50),
	PRIMARY KEY (LANGID,FUNCTIONID)
);